fs = 44100;
t = 0 : 1 / fs : 5;
w1 = sin((2 * pi * 500) * t);
w3 = sin((2 * pi * 50) * t);
w2 = -w1;
plot(t, w1 + w2);

obj = audioplayer(w1, fs);
play(obj);


%obj3 = audioplayer(w3, fs);
%play(obj3);