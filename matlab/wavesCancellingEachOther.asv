%[samples, fs] = audioread('../Sound/WAV_Long_sounds/AmChordGuitar.wav');%../Sound/Songs/Coldplay-Paradise.wav');

totalTime = length(samples) / fs;
%t = 0 : 1/fs : totalTime - 1/fs;

%w2 = w1;


%plot(t, samples(:, 1));
%yStereo = [-samples(:, 1) samples(:, 1)];
%yStereo = [zeros(length(samples(:, 1)), 1) samples(:, 1)];
%sound(yStereo, fs);

fs = 44100;
%yStereo = [w3' w1'];
%sound(yStereo, fs);